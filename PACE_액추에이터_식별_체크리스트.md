# PACE 액추에이터 식별 — 실행 체크리스트 (**biped 8-DOF / CubeMars RO100**)

> **목표**: 관절별 플랜트 파라미터 `θ = {Mₐ, d, f_c, f_v, τ_sat, q̇_max, Δ}` 를 실기서 식별 →
> MuJoCo actuator 모델을 실물과 정합 → 그 위에서 kp/kd 설계.
> **이유**: 로터관성·기어마찰·감쇠·토크-속도·지연이 sim2real 갭 #1 이다.
>
> ⚠**2026-08-10 개정.** 우리 구성은 **MD80 드라이버 + CubeMars RO100 KV55 프레임리스 모터**,
> 감속비 **hip/thigh 7 · calf 10.5 · foot 8.4**. 제어법칙 `τ = kp(q_des−q) + kd(q̇_des−q̇) + τ_ff`.
> (원본의 감속비 28/84·42/126·56/168 만 우리 값이 아니다. MD80·제어법칙은 우리 것이 맞다 —
>  한때 "다른 로봇 문서" 로 오독했으나 철회한다.)
> 아래는 우리 하드웨어와 지금까지의 실측에 맞춰 다시 쓴 것이다.

## 큰 그림
```
①측정(가진+로깅) → ②fit(회귀/CMA-ES) → 검증 → 모델통합 → kp/kd 설계
```
- **kp/kd 는 식별 대상이 아니다** — 우리가 정하는 값. 식별 대상은 플랜트 `θ`.
- 우리 구현: 로봇에서 **해석적 회귀**로 1차 식별 + `npz` 내보내기 → 노트북에서 CMA-ES 재현매칭.
  (`emb/pace/tests/act_identify_pace.py` · 로봇에 mujoco 미설치)

---

## ★우리에게만 해당하는 세 가지 (원본에 없음)

### (1) 모터 형번이 전 축 동일 ⇒ **측정이 발견이 아니라 검증**
`ROTOR_I` 는 hip 2축에서 실측 확정(7.4e−4 kg·m², HL 7.652e−4 / HR 7.121e−4).
전 축 같은 모터이므로 `Mₐ = ROTOR_I · N²` 로 **예측 가능**하다:

| 축 | N | 예측 `Mₐ` | MJCF `I_link`(HOME) | 예측 `I_total` | 링크비중 |
|---|---:|---:|---:|---:|---:|
| hip | 7 | 0.0363 | 0.2134 | 0.2497 | 85.5% |
| thigh | 7 | 0.0363 | 0.1619 | 0.1981 | 81.7% |
| calf | 10.5 | 0.0816 | 0.0288 | 0.1104 | 26.1% |
| foot | 8.4 | 0.0522 | 0.0020 | 0.0542 | **3.7%** |

⇒ 원본 Phase 1 의 *"전 가동범위 가진으로 상수 `Mₐ` 와 자세의존 `M_ii(q)` 분리"* 는
  **불필요하다.** 원본이 제시한 대안(*"CAD `M_ii` 믿고 `Mₐ = 총관성 − M_ii`"*) 경로를 쓴다.
  ⇒ **자세를 HOME 으로 고정**하고 잰다. 대신 그 자세의 `I_link` 를 반드시 재추출할 것
    (hip `I_link` 는 q=0 대비 HOME 에서 **+34.3%**, 무릎 −40° 에서 +15% 변한다).

### (1b) ★★위치처프는 **순환**이다 — `τ_ff` 가진이 아니면 θ 를 못 믿는다
드라이버가 SHM 으로 돌려주는 `fTorque` 는 **측정값이 아니라 계산값**이다:
```
q_cmd 을 10ms(실측 왕복지연 8.39ms) 정렬하면
  τ_rep = kp·(q_cmd−q) + kd·(0−q̇)  재구성 R² = 0.53 → **0.97**
```
⚠200Hz 샘플(5ms)로는 8.39ms 지연이 정렬되지 않아 R² 가 0.53 으로 낮게 나온다.
  그걸 "측정 기반" 으로 오독하기 쉽다 — **실제로 그렇게 오판했다.** 지연 정렬을 반드시 할 것.

그리고 **SHM 에 독립 전류가 없다** — `fCurrent = fTorque` 복제다(RESULTS.md:465).
MD80 은 전류센싱을 하지만 그 값이 올라오지 않는다.
⇒ 위치처프로 얻은 θ 는 **독립 검증 수단이 없다.**

**이것이 `kp` 선택 문제를 무의미하게 만든다.** kp 를 40 으로 두든 100 으로 두든 순환이다.
(참고: `spec.gains.kp 40` 은 다리 미장착 '단축 구성' 시절 가설정값이라 지금은 가진도 안 된다 —
 4Hz 에서 hip 이 명령의 31% 만 움직인다. 하지만 그건 **부차적 문제**다.)

⇒ **해법은 하나 — `kp=kd=0` 으로 두고 `τ_ff` 로 가진한다.**
  재구성할 kp/kd 항이 없으므로 순환이 **원천 소멸**한다. 우리가 넣은 τ_ff 가 곧 입력이다.

**⚠분해는 필요 없다.** 원본이 말한 '1축 격리' 는 *"조립 상태로 한 축씩 + 나머지 축 잠금"* 이다:
| | 게인 |
|---|---|
| **가진축** | `kp=kd=0` · `τ_ff = τ_g(자세) + 처프` |
| **홀드축** | 배포 게인 유지(hip100/thigh50/calf50/foot30) |

⚠**가진축은 복원력이 0 이다** — `τ_g` 추정이 틀리면 그대로 흘러내린다. 그래서:
- **중력보상(`τ_ff = τ_g`)이 관성측정의 전제**다. 순서가 뒤집힌다: ②중력보상 → ①관성측정
- 무충격 전환(bumpless): `kp` 를 내리면서 `τ_ff` 를 동시에 올린다
- **위치 워치독**: `|q − q0| > 임계` 면 즉시 hold 복귀
- foot 부터 하는 이유가 하나 더 — 중력토크가 0.03~0.17 Nm 로 가장 작아 추정오차 노출이 최소다
  (hip 은 4.96 Nm)

### (2) 드라이버 감속비 오설정 ⇒ **식별값은 채널계다**
드라이버가 전 축을 7:1 로 가정해 보고한다(실제 calf 10.5 · foot 8.4).
PACE 는 채널각·보고토크로 회귀하므로 `q_ch = k·q_joint`, `τ_ch = τ_joint/k` 에서
```
τ_ch = (I_joint / k²) · q̈_ch      ⇒  식별값 × k² 라야 관절계
k: hip·thigh 1.0 · calf 1.5 · foot 1.2   (calf ×2.25 · foot ×1.44 복원)
```
`emb/pace/prep_inertia.py --identified <값> --axis <축>` 이 복원·`ROTOR_I` 역산·판정을 한다.

### (3) calf→foot 기구 커플링
foot 을 가진하면 전치 관계로 calf 모터가 `τ_foot` 을 떠안는다(`τ_raw_calf = τ_calf − coef·τ_foot`).
측정 후 `check_hold` 결과를 값과 **함께** 기록할 것.

---

## Phase 0 — 준비
- [x] `kp=0, kd=0` (raw torque) 모드 **허용** 확인 ✅ — 단 **실기 구동은 아직 0회**
- [ ] ★`τ_ff` 가진 경로 실기 검증 (중력보상 ②가 그 첫 시험)
- [x] 스케일 상수: `N`, `Kt`(1.26/1.89 Nm/A, **관절측**), 엔코더, 데이터시트 `τ_peak`·`q̇_max` — `emb/pace/spec.yaml`
- [x] 지연 `Δ` 실측 8.39 ± 0.79 ms (`act_measure_latency.py`)
- [ ] **지그로 영점 재캘리브레이션** → `prep_inertia.py` 재실행으로 그 자세 기준값 재산출
- [ ] **1축 격리** — 조립 상태로 한 축씩 + 나머지 고정. ★우리는 여기서 원본보다 더 들어갔다:
  - 전기적 홀드는 **스프링**이라 공진 `f_n = √(kp·k²/I)` 위에서 자유처럼 행동한다.
    `kp_max 100` 에서 hip 3.19 · thigh 2.53 · calf 5.08 · foot 4.49 Hz.
  - **기구 클램프의 두 효과를 구분할 것**:
    ① 홀드민감 개선 → **인접(원위) 축**을 묶는다 (thigh 면 calf·foot)
    ② 대역폭 확보 → **공진 낮은 축**을 묶는다 (hip·thigh)
    ⚠hip 을 묶어도 thigh 홀드민감은 −56.2 → −56.1% 로 **안 좋아진다**(결합은 인접에서 온다).
  - ⚠클램프는 **강성이지 관성이 아니다** — 지그 질량이 링크에 붙으면 측정이 오염된다.
- [ ] **안전장치**: 하드스톱 + E-stop. ⚠토크 트립 `15 Nm`(50ms 연속)·속도 `200 dps` 는 **래치**이고
      **off 명령으로만 풀린다**. hip·thigh 관절한계는 아직 물리 하드스톱과 대조된 적이 없다.
- [x] **로깅**: `q`·`q̇`·`τ_rep`·**`I`(전류)**·`q_cmd`. `rate_hz` **500**
      (원본은 ≥1kHz 를 요구하지만 요구 샘플레이트는 **가진 대역폭에 비례**한다 — 아래 Phase 1)

## Phase 1 — 가진 궤적 (**`kp=kd=0` + `τ_ff`. 위치처프는 순환이라 쓰지 않는다**)
- [x] chirp — **0.2 ~ 4 Hz**. ⚠원본의 `0.1~20 Hz` 는 **우리 로봇에서 불가능**하다:
      · 홀드 공진이 2.5~4.5 Hz 라 그 위는 홀드가 무너진다
      · 다리 장착으로 등가관성이 6.9배 → 20 Hz 안전진폭이 foot 기준 **0.27°**
      ⇒ 4 Hz 상한. 500 Hz 샘플이면 한 주기당 125샘플로 충분하다.
- [ ] 진폭: **축별 안전값**. ⚠spec 의 `amplitude_deg 6.0` 은 **다리 미장착 시절** 값이라
      4 Hz 에서 hip 16.5 · thigh 13.1 Nm 로 트립(8 Nm)을 넘는다.
      트립 50% 기준 안전진폭: hip 1.45 · thigh 1.83 · calf 3.29 · **foot 6.69°**
- [ ] 저속 반전 + 속도 0 교차 → `f_c` (`act_measure_friction.py` 가 양방향 상쇄로 분리)
- [ ] step/구형파 → `Δ` (`act_measure_latency.py`)
- [ ] ~~고속 + 포화 근처 → `τ_sat`, `q̇_max`~~ **범위 밖**. 다리 달린 매단 상태에서 안전하게
      도달할 수 없다. 데이터시트 값(`τ_peak` 84/84/126/100.8 · `q̇_max` 29.6/29.6/19.7/24.6 rad/s)을
      쓰고 **벤치 전용**으로 표기한다.
      ⚠`τ_peak` 은 순환일 여지가 있다 — 8축 모두 `÷gear = 12.0 Nm` 로 데이터시트와 정확히
        일치한다. 애초에 `12×N` 으로 만든 값일 수 있으니 모터 라벨을 눈으로 확인할 것.
- [ ] ~~전 가동범위 가진~~ **불필요** — 위 (1) 참조. HOME 고정.

## Phase 2 — 데이터 수집 (★순서 고정)
아래에서 위로. 끝난 축을 클램프해 가며 올라간다.

| 순서 | 축 | 클램프 | 홀드민감 | `f_end` | 무엇을 얻나 |
|---|---|---|---:|---:|---|
| ① | **foot** (ch3·7) | 불필요 | −1.2% | 4 Hz | **`ROTOR_I` 확정**(로터 96.3%)·측정체계 검증 |
| ② | **hip** (ch0·4) | 불필요 | −1.5% | 4 Hz | **MJCF `I_link` 검증**(링크 85.5%) |
| ③ | calf (ch2·6) | foot+thigh | −19.3 → **−0.1%** | 4 Hz | `JDAMP`·`JFRIC` |
| ④ | thigh (ch1·5) | calf+foot | −56.2 → **−0.0%** | 4 Hz | 〃 (가장 어려움) |

- [ ] 각 축 **반복 2회** 이상 (재현성). 가능하면 warm/cold 비교
- [ ] **hold-out 궤적** 1개 별도 수집 (fit 미사용, 검증 전용)
- [ ] 로그 저장: `pace_dataset_ch##.npz` — `t·q·dq·tau·cur·q_cmd`

## Phase 3~4 — fit
- [ ] 모델식 (`act_identify_pace.py` 구현):
      `τ = I_total·q̈ + b·q̇ + τ_c·tanh(q̇/ε) + A·sin(q) + c`
      ⚠`cos(q)` 는 넣지 않는다 — 소진폭에서 상수열과 공선(조건수 16348→365).
        얻어지는 `A` 는 절대중력이 아니라 **"가진범위 내 중력 변화폭"** 이다.
- [ ] **★전류 교차검증** — `corr(τ_rep, Kt·I)`. 원본이 경고한 "보고토크가 kp/kd 재구성이면 순환"
      을 가르는 유일한 독립 근거다.
      *(기존 hip 데이터 검증 결과: MIT 재구성 R² **0.53** — 절반이 설명되지 않는다 ⇒
        보고토크는 측정 기반이다. 그래도 축마다 다를 수 있어 매 측정에서 확인한다.)*
- [ ] 초기값·경계 = 위 (1) 표의 예측값
- [ ] npz → 노트북 CMA-ES 재현매칭

## Phase 5 — 검증
- [ ] **hold-out 예측오차**
- [ ] **물리 sanity**: `Mₐ ≈ ROTOR_I·N²`(hip 실측 기준) · `f_c/d` 합리범위
- [ ] **잔차 계통분석** — R² 만 보지 말 것. 위치의존 마찰·코깅은 계통 잔차로 드러난다
- [ ] `check_hold` 통과 여부를 **값과 함께** 기록 (홀드가 밀렸으면 그 값은 자유 쪽으로 편향)

## Phase 6 — sim 통합
- [ ] MJCF 에 주입: `armature = Mₐ` · `damping = d` · `frictionloss = f_c`
      ⚠**MJCF 에는 이 셋이 한 줄도 없다** — 전부 런타임 코드가 주입한다.
        RL 팀이 MJCF 만 로드하면 반사관성·마찰 0 인 로봇을 학습한다(`emb/RL_INTERFACE.md` §6-a).
- [ ] 지연 `Δ` 액추에이터 지연 모델
- [ ] **비물리 캡 금지** — 한계는 물리 τ-ω 곡선으로만
- [ ] 같은 가진 재생 → 실측 매치

## Phase 7 — kp/kd 설계
- [x] `kd = 2ζ√(kp·I_total) − JDAMP`, ζ ≈ 0.7~0.8 → hip 6.0 · thigh 4.0 · calf 3.5 · foot 2.0
- [ ] 식별값으로 `I_total` 이 바뀌면 **kd 재산출**
- [ ] ⚠실효 관절강성은 `kp·k²` 다(감속비 오설정). calf 50 → 112.5 · foot 30 → 43.2.
      드라이버가 고쳐지면 calf·foot 이 갑자기 약해지므로 그때 재튜닝.

---

## 핵심 원칙 · 함정
- **kp/kd ≠ 식별 대상.**
- **전류 로깅으로 순환 여부를 판별**한다(우리 hip 데이터는 통과).
- **순수토크(kp=kd=0) 폭주** → 하드스톱·트립. 트립은 래치이고 off 로만 풀린다.
- **관절종류별 개별 식별** — 감속비가 다르다.
- **격리는 전기 홀드로 완전하지 않다** — 홀드는 스프링이고 공진 위에서 무너진다.
  인접 축을 기구적으로 묶어라.
- **식별값은 채널계** — `× k²` 복원을 잊지 말 것.
- 데이터시트는 초기값/경계, 실측이 최종. 단 `τ_peak` 은 순환 가능성 있음.

## 산출물
- 축별 `θ*` 표 (`Mₐ`, `d`, `f_c`, `f_v`, `Δ`) + 클램프 조건·`check_hold` 결과
- 갱신된 MJCF actuator + hold-out 검증 리포트
- 재산출된 kp/kd
