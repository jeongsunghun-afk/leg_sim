# 공유 발견 — biped ↔ quad

biped 와 quad 를 서로 다른 세션에서 개발 중이라, **양쪽에 모두 해당되는 내용만** 여기 모은다.
한쪽에만 해당되는 것은 각 프로젝트 문서에 둔다(biped: `biped/emb/pace/RESULTS.md`,
`biped/cpp/STABILITY_MAP.md`).

| 버전 | 날짜 | 작성 | 내용 |
|---|---|---|---|
| v1 | 2026-08-05 | biped 세션 | 야코비안 평가점 버그 · 노이즈 규약 통일 · 앵커 한계 · 지표 함정 |

---

## 1. 🔴 leg-odometry 야코비안 평가점 불일치 — **양쪽 모두 있는 버그**

### 어디에

| 프로젝트 | 파일 | 줄 | 경로 |
|---|---|---|---|
| quad | `quad/cpp/src/state_estimator.hpp` | **55–56** | `estimate()` leg-odom |
| quad | `quad/cpp/src/state_estimator.hpp` | **134–135** | `estimate_kf()` 접촉 KF |
| biped | `biped/cpp/src/state_estimator.hpp` | 53–56 | `estimate()` — **수정 완료(스위치)** |

세 곳 모두 같은 형태다:

```cpp
Vector3d pfb(geom_xpos[3*g], geom_xpos[3*g+1], geom_xpos[3*g+2] - foot_rad[k]);  // 접촉점
mjtNum pnt[3]={geom_xpos[3*g], geom_xpos[3*g+1], geom_xpos[3*g+2]};              // ← 구 중심
mj_jac(m, ed, jb.data(), nullptr, pnt, m->geom_bodyid[g]);
...
vbs.push_back(-(omw.cross(rel) + R*vfb));   // rel 은 접촉점, vfb 는 중심 속도
```

**위치는 접촉점, 속도는 구 중심**에서 계산한다.

### 왜 틀렸나

정지접촉 조건은 "**접촉점**의 world 속도 = 0" 이다:

```
v_P^world = v_base + ω_base × (R·p_P) + R·(J_P·dq) = 0
        ⇒  v_base = −( ω × R·p_P + R·J_P·dq )
```

`p_P` 와 `J_P` 는 **같은 점 P** 여야 한다. 다른 점을 쓰면 오차가 남는다:

```
오차 = R·(J_contact − J_center)·dq = R·( ω_발 × (−r·ẑ) )     크기 ≈ |ω_발| · r
```

스탠스 동안 발 각속도의 부호가 일정하므로 **랜덤 오차가 아니라 계통 편향**이다.

### 크기

| | 발 반경 r | \|ω_발\| 스탠스 중 | 편향 |
|---|---|---|---|
| biped | 0.036 m | ~2.6 rad/s | **~0.094 m/s** |
| quad | 0.025 m | (미확인) | r 에 비례 → biped의 0.69배 |

biped 지령속도 0.15 m/s 대비 **60% 급**이다.

### biped 실측 효과 (배포조건 vx=0.10 · ACT 지연 8.4 ms + 보상)

| | 실속도/지령 | 추정−실제 | falls |
|---|---|---|---|
| 중심(기존) | 1.085 | −0.250 m | 0 |
| **접촉점(수정)** | **0.965** | **−0.020 m** | 0 |

**위치 드리프트 92% 감소.** 편향의 지배적 원인이 이것이었다.

### ⚠ 고칠 때 반드시 같이 할 것 — 재튜닝이 필수다

biped 에서는 수정만 하고 튜닝을 그대로 두자 vx=0.15 낙상이 1→4 회로 **늘었다**.
저역통과(alpha)를 세게 걸어도 회복되지 않았다(대조군도 동일하게 악화 → 필터 문제가 아님).

**기존 보행 튜닝이 이 편향을 전제로 맞춰져 있었다.** 편향이 사라지면 발디딤 게인이 보던
오차의 성격이 바뀐다. ⇒ 추정기 수정과 **발디딤 게인 재튜닝은 반드시 짝으로** 가야 한다.

biped 재튜닝 결과(실측 센서노이즈 + 배포지연 8.4 ms, 3 시드 × 30 s):

| 설정 | vx 0.10 | vx 0.15 |
|---|---|---|
| 구 `jac=중심 · T_STEP 0.32 · K_RETURN 0.45` | falls 1/2/2 | falls 2/2/2 |
| **신 `jac=접촉점 · T_STEP 0.38 · K_RETURN 0.15`** | **falls 0/0/0** 속도비 0.983~0.993 | **falls 0/0/0** 속도비 0.936~0.987 |

**구 설정은 실측 노이즈를 넣으면 6/6 전부 낙상했다.** 노이즈 없이 얻은 안정영역 지도는
버그를 고치기 전에도 후에도 신뢰할 수 없다 — quad 쪽도 노이즈 포함 재검증을 권한다.

> **sim2real 위험**: 편향 크기가 **발 구 반경에 비례**한다. 시뮬 튜닝이 이 편향에 기대고
> 있으면 실기 발 형상이 다른 순간 그 튜닝은 옮겨가지 않는다. 이게 이 버그를 고쳐야 하는
> 진짜 이유다 — 정확도보다 **전이 가능성** 때문이다.

### biped 쪽 상태

`BipedEstimator::jac_at_contact` + `EST_JAC_CONTACT` env 로 A/B 가능하게 해 뒀다.
**biped 는 재튜닝까지 마치고 기본값을 `true`(접촉점)로 전환했다.**
quad 에 옮길 때는 **기본 `false` + env 토글로 먼저 넣어** 기존 회귀 결과를 깨지 않고
비교한 뒤, 재튜닝이 끝난 다음에 기본값을 바꾸는 순서를 권한다.

---

## 2. 센서 노이즈 규약 — biped 를 quad_ctrl 규약에 맞춤

biped `deploy_loop.hpp` 는 **스칼라 `NOISE` 하나**로 q/dq/gyro 노이즈를 동시에 스케일했다:

```
σ_q = NOISE·0.005 rad   σ_dq = NOISE·0.05 rad/s   σ_gyro = NOISE·0.02 rad/s
```

이 규약은 `σ_dq/σ_q` 비를 **10 [1/s] 로 고정**한다. biped 실기 실측은 **482 [1/s]** 로
**48배 어긋난다**. 스칼라 하나로는 두 채널을 동시에 못 맞춘다(위치 기준 NOISE=0.015 vs
속도 기준 0.736).

⇒ `quad_ctrl/hal/mujoco_hal.hpp` 규약대로 **`ENCQ_N`/`ENCDQ_N`/`GYRO_N` 절대 sigma**로
분리했다. 미지정 시 기존 `NOISE` 배율로 폴백해 하위호환 유지.
**이제 두 프로젝트가 같은 env 이름·같은 단위를 쓴다.**

### biped 실측값 (2026-08-05 · HL/HR hip · limp 8초 · 각 1545 샘플)

| | biped 실측 | quad_ctrl config 대표값 | 비 |
|---|---|---|---|
| `ENCQ_N` [rad] | **7.64e-05** (0.0044°) | 0.002 | biped 가 **26배 깨끗** |
| `ENCDQ_N` [rad/s] | **0.0368** (2.11 °/s) | 0.02 | biped 가 1.8배 지저분 |
| `GYRO_N` | **측정 불가** | 0.02 | — |

- **위치가 규약 가정보다 훨씬 깨끗하다.** 노이즈를 규약값으로 넣고 튜닝하면 실기보다
  가혹한 조건에서 튜닝하는 셈이다.
- **속도는 반대로 더 지저분하다.** 속도가 위치 차분에서 나오므로 당연한 결과다.
- σ 와 peak-to-peak 을 구분할 것. biped dq 의 p-p 는 14.6 °/s 지만 **σ 는 2.11 °/s** 다.

> ⚠ **`GYRO_N` 은 근거 없는 값이다** — biped 실기 IMU 가 전부 0 으로 들어온다(아래 4절).
> quad 실기에서 IMU 가 살아 있다면 **자이로 노이즈 실측값을 여기 공유해 달라.**
> biped 는 지금 측정할 방법이 없다.

---

## 3. stance 앵커는 누적 드리프트를 못 지운다 — 기대치 조정

양쪽 구현이 동일하게 **접촉 개시 시점에 현재 추정치로 앵커를 등록**한다:

```cpp
if(!anch_on[k]){ anchor[k] = p + rel; anch_on[k]=1; }   // quad :60 / biped :70
```

"점프 없음"을 위한 의도적 설계지만, 결과적으로 **앵커가 그 시점까지 쌓인 오차를 그대로
물려받는다.** 따라서 앵커링이 막아주는 것은 **스탠스 한 구간 내부의 적분 드리프트**뿐이고,
걸음을 넘어 누적된 오차는 원리적으로 못 지운다.

biped 실측(청정조건, vx=0.15, 20 s):

| k_anchor | 실속도/지령 | 추정−실제 | falls |
|---|---|---|---|
| 0 (off) | 1.143 | −0.430 m | 0 |
| 0.50 | 1.133 | −0.460 m | 0 |
| 1.00 | 1.130 | −0.450 m | 0 |
| 0.05 / 0.20 | — | — | **6 / 1** |

- **거의 효과 없다.**
- **중간 게인이 오히려 낙상시킨다** — `p` 만 밀고 `v` 는 안 고쳐서 둘이 불일치해진다.
  앵커 게인을 도입할 거면 p/v 를 함께 보정하는 형태여야 한다(= KF).

**속도 편향은 앵커로 못 고친다.** quad 주석의 성과를 구분해 읽을 것:
"z 1.2 m 폭주" 해결은 **위치** 수정(앵커/접촉높이)이고, "114°→0.3°" 는 `KF_QV`, 즉
**속도 채널** 수정이다.

---

## 4. IMU 관련 (biped 쪽 상황 공유)

biped 실기 IMU 는 **완전히 죽어 있다.** SHM(key 1234) 을 직접 떠보니 `fIMUBuf` 102 float
전부 0 인데 `IsUpdatedIMU=1` — **가짜 신선도**다. Emb 3중 결함:

1. `defineGeneral.h:44` `DEFINED_HEAD_SIMULATOR_ENA` 로 IMU UART open 이 컴파일에서 빠짐
   (⚠ 이 매크로가 초기화 카운터·자세계산 입력까지 함께 묶고 있어 **그냥 끄면 실기가 안 뜬다**)
2. `engRobot.cpp:416/465` 가 매 주기 로컬 0 배열을 SHM 에 씀
3. 성공 경로도 ARPY(10-12)·ACCL(4-6) 만 복사하고 **GYRO(7-9)·QUAT(0-3) 은 안 채움**

4. `halIMU.cpp:164-167` 이 파싱 결과를 `[unDevID]` 가 아니라 **`[0]` 행**에 쓰는데
   게터는 Head=3/Body=4 행을 읽는다 → UART 를 열어도 영구 0
   (바로 아래 169행 `m_fTemperature[unDevID]` 만 정상이라 **오타 확실**)

여기에 우리 브리지가 **SDK 에 존재하지 않는 심볼** `IDX_OF_IMU_AVEL` 을 써서
`#ifndef` 폴백이 `-1` 로 정의되고 사용처가 컴파일 타임에 접혀 자이로가 상수 0 이었다
(실명은 `IDX_OF_IMU_GYRO`=7). ⇒ **tilt E-stop 이 완전히 무력**(tilt≡0 이라 임계 도달 불가).
**우리 쪽은 수정 완료**(심볼 교체 + "신선한 0" 방어 + 재빌드, 기계어 확인).

⚠ **공유할 함정 두 가지**
- **"신선한 0" 이 값 0 보다 위험하다.** `SetIMU` 가 내용 검증 없이 `ucIsUpdated_IMU=1` 을
  세워서 freshness 검사로 안 걸러진다. 방어법: 정상 IMU 는 정지 중에도 가속도에 중력
  ~9.81 m/s² 가 잡히므로, `|a|≈0` 이면 무효로 판정하면 된다.
- **존재하지 않는 매크로에 `#ifndef` 폴백을 두지 말 것.** 경고 없이 조용히 기능이 사라진다.
  `#error` 로 바꿔야 빌드가 잡아준다.

**quad 세션에 요청**: 같은 Emb 스택을 쓴다면 위 결함이 quad 에도 있을 가능성이 높다.
확인해 보고, **IMU 를 살린 경험이 있으면 공유해 달라.** IMU 없이는 양쪽 모두
가속도 융합 KF 로 갈 수 없다.
biped 상세 진단·요청 문구 초안은 `biped/emb/IMU_RECOVERY.md`.

---

## 5. 지표 함정 — 낙상 횟수만 보면 보행 실패를 놓친다

`biped_control.hpp:234` 의 `reset()` 은 낙상 시 `d->qpos[i]=0` 으로 **로봇을 원점으로
되돌린다**(quad 도 동일 구조라면 같음). 따라서:

- **이동거리·평균속도 지표는 `falls=0` 인 런에서만 유효하다.** 낙상이 섞이면 마지막
  리셋 이후 거리만 남는다.
- 더 중요한 건, **낙상 0 이어도 보행이 실패일 수 있다.** biped 는 `falls=0` 인 조건에서
  실속도가 지령의 **1.14배**였다. 낙상 지표만 봤으면 "정상"으로 집계됐을 것이다.

**권장 지표**: `falls` + **실속도/지령 비**(1.0 이 완벽) + `tilt`. 셋을 같이 볼 것.
biped 는 tilt 45° 임계도 관대하다고 판단해 "품질" 판정은 tilt ≤ 10° 로 따로 본다.

---

## 6. 참고 — biped 실기 계측 방법론

액추에이터 특성 추출(PACE) 하네스가 `biped/emb/pace/` 에 정식화돼 있다.
로봇이 달라도 **방법과 실패 사례는 그대로 옮겨갈 수 있다**:

- `hwio.py` — 안전 SHM I/O(전 여기를 통과). 신호처리 + `atexit` + `limp()` + **드라이버
  생존 확인**(`verify_driver_live()`). ⚠ EtherCAT/텔레메트리가 멀쩡한데 드라이버 전력단만
  latch off 인 상태가 실재한다 — stale 검출만으로는 못 잡는다. 이 함정에 한 번 빠져
  "순수 토크모드 미지원" 이라는 **틀린 결론**을 냈었다.
- 정지마찰은 **속도 임계가 아니라 위치 변위**로 잡을 것. 속도 노이즈 p-p 14.6 °/s 라
  속도 판정은 불가능하다(τ_s < τ_c 라는 물리적으로 불가능한 결과가 나왔었다).
- 백래시/강성은 **반복 측정 평균 + warmup 사이클 제외** 필수. 안 하면 산포 44~49%,
  하면 1~2% 로 떨어진다(초기 기계적 안착 때문).
- 강성에서 **게인 상한이 관성과 무관하게 나온다**: `Kp_max = k/9`. 재튜닝 출발점으로 유용.

자세한 값과 실패 기록은 `biped/emb/pace/RESULTS.md`.
