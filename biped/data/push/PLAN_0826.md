# push 실험 계획 — 4원 연립으로 경로 확정 (2026-08-25 저녁 재설계)

★재설계 이유(08-25 실측): **발끝밀기는 두 점 접촉이 유지되지 않는다** — 힘을 발끝
자코비안으로 걸면 정역학상 뒤꿈치 힘이 0 이라, 손실 잔차가 발목을 돌려 뒤꿈치를
띄운다(② 시도에서 +6→+22 mm, 발목 −60→−53.5°). 심으로는 못 잡는다. 그래서
"전체접촉 스윕"을 폐기하고 **뒤꿈치밀기(PUSH_POINT=heel)** 와 **Qhome8 발끝밀기**
로 대체한다.

## 연립 구조 — 미지수 r = (hip, thigh, calf, foot) 경로 전달비

```
  E1 (0° toe, 확보 ✓):     0.25·hip + 0.72·calf              = 0.82
  E2 (평발 toe=③, HL ✓):   0.20·hip + 0.30·thigh + 0.50·foot = 0.783
  E3 (평발 heel, 신규):     0.47·hip + 0.53·calf              → E1 과 연립, hip/calf 분리
  E4 (Qhome8 toe, 신규):    0.48·hip + 0.24·thigh + 0.28·foot → E2 와 연립, thigh/foot 분리
                            (thigh:foot 비 0.86 vs E2 의 0.60)
```

가중은 매 스윕 기록된 q 로 노트북에서 재계산한다(위 숫자는 설계용 근사).

⚠ 공통: 로봇 고정 · GUI 끄고 push_scale 실행 · 기입은 "저울 5초 ±10 g 안정" ·
유격은 심으로 저울을 올려 100~300 g 창 · 각 스윕 후 JSON 커밋·푸시
(`git pull --rebase && git push` 패턴).

---

## 순서 A — ③ HR: 평발 발끝만 (HL 은 08-25 완료, blend 0.783)

```
  ./run_deploy_hw.sh flat
  python3 tools/push_scale.py --leg HR --max 30 --step 5
  저울: 발끝만 올라가게(뒤꿈치는 모서리 밖 허공·발은 수평 그대로)
```

## 순서 B — ② 재설계: 평발 뒤꿈치밀기 (E3) — ★재빌드 후

```
  배포기 Ctrl+C → PUSH_POINT=heel ./run_deploy_hw.sh flat
  python3 tools/push_scale.py --leg HL --max 30 --step 5
  python3 tools/push_scale.py --leg HR --max 30 --step 5
  저울: 발바닥 전체가 저울 위면 됨(뒤꿈치 심 불필요 — 접촉이 뒤꿈치로 자기수렴)
  PUSH_DBG 배너에 "다리 HL·뒤꿈치" 로 표기되는지 확인
```

예측: α공통이든 축별이든 **0.83** (hip·calf 만 실림) — 0.83 에서 벗어나면
hip/calf 배분이 종전 해석과 다르다는 새 정보다.

## 순서 C — ④ Qhome8 발끝밀기 (E4) — thigh/foot 최종 분리

```
  yaml home = Qhome8 원복됨(이 커밋) — 1점 모드가 이걸 읽는다
  배포기 Ctrl+C → ./run_deploy_hw.sh        (1점·발끝밀기 기본)
  python3 tools/push_scale.py --leg HL --max 30 --step 5
  python3 tools/push_scale.py --leg HR --max 30 --step 5
  ⚠Qhome8 발끝은 0° 때보다 **64 mm 낮다** — home 도달 후 저울을 놓을 때 심을
    그만큼 빼라(도구가 "저울 배치 → 엔터" 흐름이라 이동 중 충돌은 없음)
```

## 각도 시험(구 ①) — ★완료·종결

비(比): calf jog 때 foot 부동(커플링 상쇄) = calf·foot 배율비 설계값 정합.
절대치: 평발 home(foot −59.8° 이동)에서 밑창 실수평 — 배율 오차면 그대로 보였을 것.
⇒ calf·foot 각도 경로 확정, 추가 시험 없음.

## 끝나면

JSON 전부 커밋 → 노트북에서 스윕별 실측-q 가중으로 E1~E4 최소자승
→ r 4원 확정 → run_deploy_hw.sh 의 foot 1.00 갱신 + 아티팩트·메모리 최종 반영.

---

## 리허설 시뮬 결과 (08-26, tools/push_sim_rehearsal.py — 실기 전 검증 완료)

배포기 push 법칙 + r 손실 + 조립마찰(0.7 Nm)을 고정베이스 MuJoCo 로 재현:

| 시나리오 | 결과 |
|---|---|
| C. 뒤꿈치밀기(E3) r공통 | ✅ 접촉이 뒤꿈치로 자기수렴(toe 0.1 N/heel 25 N) · 기울기 0.80 재현 · foot 폭주 없음 |
| D. E3 + r_foot 0.66 | ✅ 기울기 0.80 그대로 — **E3 는 foot 에 무감** (설계 목적 달성) |
| E. Qhome8 발끝(E4) | ✅ 한 점 안정 · 기울기 0.80 · 자세 크리프 소폭(실기와 동급) |
| A/B. 발끝밀기 재현 | ⚠깨끗한 모델에선 실기 ②의 뒤꿈치 떠오름(+12° 회전)이 **재현 안 됨** (r_foot 결손 0.66 으로도 발목 ≤1°) — 실기의 회전은 모델에 없는 발목토크 잔차(레버 오차/α_foot/저울 모서리) 시사. E3 무감성 덕에 E3 에는 무영향 |

★해석 파이프라인 준비 완료: `tools/push_solve.py` — 미는점 자동판별(τ 기울기 vs 자코비안),
전이·오염·고력처짐 자동 마스킹, 다리별 가중 최소자승 + 미식별 방향 정직 보고.
가상 E3·E4 자가시험 통과. JSON 커밋만 하면 즉시 최종값이 나온다.
⚠기대치 관리: E3·E4 후에도 **thigh·foot 의 개별 분리는 ±0.3 수준 약식별** —
hip·calf 는 단단히 박히고, 운용점 blend(E4)는 그 자체로 정밀하다.

---

## ★최종 결과 (08-26 저녁 — 캠페인 완주)

| 자세 | HL | HR | 결론 |
|---|---|---|---|
| 0°·평발 (E1·E2·E3) | 0.789~0.836 | 0.779~0.858 | **blend ≈ 0.80±0.02** |
| Qhome8 (E4) | 0.660±0.068 | 0.694±0.059 | **≈ 0.68±0.05 — 양다리 재현** |

**고정 축별 r 모델은 기각** — 전달비가 자세를 탄다. E3(hip+calf) 0.84~0.86 으로
hip·calf 는 건강 ⇒ Qhome8 결손의 유력 국소화 = thigh(HL 케이블 확정분) +
**foot 의 발목각 의존 손실**(0° 부근 ~0.4~0.5 vs −55° 에서 ~0.8 — E2 정합).
운용: 평발 계열 0.80(현행 유지) · Qhome8 힘제어 0.68(λ 보정 ×1.47, walk 시 결정).
격리 3건: 185036(두점 힘고리)·192147(테두리 누설)·꼭짓점 비단조(자동).
다음 확증: 발목각별 foot 단독 토크시험 · 포렌식 배너(틱/축사망) · 발기하 3.4° 오차.

---

## ★무게추 브래킷 캠페인 결과 (08-27 완주 — WEIGHTED_FOOT_PLAN.md 의 실행 기록)

| 창(발목 관절각) | G 신호 | HL | HR |
|---|---|---|---|
| A (0°) | 1.43 Nm | 0.52 / 0.54 | 0.541 |
| C (−25°) | 2.11 Nm | 0.645 | 0.615 |
| B (−50°) | 1.83 Nm | 0.563 | — |

**r_foot(G) = α − k/G · α=0.77±0.08 · k=0.36±0.12 Nm (rms 0.021) — 좌우 동일.**
각도의존 기각 — "자세의존"의 실체는 토크크기 의존(상수 결손). push E2/E4 와 정합.
운용 함의: 고부하 접촉(지지) ~0.77 · 저부하 미세조절 0.5~0.6 →
보정은 스케일("×1.47")이 아니라 **foot 드라이브 sign(τ)·k 전방보상**이 정칙.
다음: 보상항 구현(C++·sim 파리티) → sim 배터리 재검증 → 실기 검증.
원자료: data/push/float_gstar_*.json (4개 · 전 8축 표류 포함).
