# 액추에이터 식별 — 최종값 (2026-08-14)

`fit_v5` 기준. 각 값은 **가장 믿을 만한 출처 하나**를 골라 적었다.
신뢰도는 **네 번의 적합(v2·v3·v4·v5)에서 얼마나 흔들렸나**로 매겼다 — 한 번의 오차막대보다 그게 정직하다.

---

## 0. 무엇을 채웠나 — 수식으로

한 축이 따르는 식은 이것이다. **각 항이 어느 파라미터고, 무엇으로 채웠는지** 표시했다.

```
 τ_cmd(t − T_d)  =  [ I_link + I_rot·N² ]·q̈  +  b·q̇  +  τ_c·sgn(q̇)  +  g(q)
      └── ⑥ ──┘      └─ ① ─┘  └─ ② ─┘         └ ③ ┘    └──── ④ ────┘   └─ ⑤ ─┘
                     └──── dof_armature ────┘   dof_damping  dof_frictionloss

 그리고 발목만:   q_raw = q_foot + coef·q_calf   ← ⑦    (엔코더·명령·토크 전부 이 좌표)
 관측:            q_enc = q_true + q̃_b          ← ⑧
```

| | 항 | 무엇으로 채웠나 | 상태 |
|---|---|---|---|
| ① | `I_link` 링크 관성 | **CAD** — 실측으로 검증 | ✅ calf·foot ±8.3% · thigh·hip 미검증 |
| ② | `I_rot·N²` 로터 반사관성 | **각축 토크시험** ROTOR_I 7.327e-4 | ✅ 4곳 ±10% |
| ③ | `b` 점성 | foot 은 **각축**, 나머지는 PACE | ⚠ foot ✅ · thigh 보통 · calf·hip ❌ |
| ④ | `τ_c` 쿨롱마찰 | **PACE** (각축과 25% 차) | ⚠ ③과 맞바꿔져 나눔이 안 됨 |
| ⑤ | `g(q)` 중력 | **중력표 + 자세별 실측보정** | ✅ 시험 내부용 (MJCF 는 CAD 가 계산) |
| ⑥ | `T_d` 지연 | **직접 실측** 8.39±0.79 ms | ✅ 좋다 |
| ⑦ | `coef` 커플링 | **관성 대조** 비 1.06 · PACE 자유탐색으로 재확인 | ✅ 1.0 유지 (자유로 풀면 성적이 나빠짐) |
| ⑧ | `q̃_b` 엔코더 영점 | **PACE** 축별 8개 | ✅ 최대 0.52° |

**⇒ ①②⑥⑦⑧ 은 닫혔다. ③④ 는 총량만 맞고 나눔이 안 됐다.**
③④ 가 갈리려면 **속도를 독립 제어하는 시험**이 필요하다(§4-②).

⚠MuJoCo 는 이 식 그대로가 아니다. `τ_c·sgn(q̇)` 만 있고 **정지마찰(Stribeck) 항이
없다** — 실측에서 운동마찰이 정지마찰의 72~98% 였는데 그 차이를 표현할 수 없다.
도메인 랜덤화로 덮는 게 현실적이다.

---

## 0-b. 왜 각축이 필요했나 — 다축 PACE 의 세 한계

**출발점은 다축이다.** 8축을 동시에 처프로 흔들어 궤적을 재현하도록 파라미터를 맞춘다
(`collect_multichirp` → `pace_cmaes`). 자연스러운 방법이고, 실제로 대부분을 이걸로 정했다.
그런데 **다축만으로는 원리적으로 못 가르는 것이 셋** 있었고, 각축은 그걸 메우려고 붙였다.

> **먼저 말 두 개.** 다축 최적화(CMA-ES)는 무에서 값을 찾지 않는다.
> **어딘가에서 출발해서**(= **초기값**), **정해둔 범위 안만** 뒤진다
> (= **탐색범위**). 둘 다 각축 실측에서 왔다 — 그게 각축이 다축을 도운 방식이다.
> 탐색범위 끝까지 밀려 멈춘 값은 **찾은 값이 아니라 더 못 간 값**이다(표의 ★).

### 한계 ① 두 값을 맞바꿔도 티가 안 난다 — 궤적으로는 **원리적으로** 못 가른다

`JDAMP.foot` ↔ `JFRIC.foot` 상관 **r = +0.93**. 점성을 올리고 마찰을 내리면 궤적이
거의 같아진다. 그래서 둘의 합만 정해지고 **나눔이 안 된다**(남은 불확실 −66 ~ +64%).

> `design_excitation.py` 가 **설계 단계에서 이미 예측했다.** 궤적 쪽 처방
> (dual·f0scale·f1scale)을 전부 시도했지만 실패했다 — 가진을 어떻게 설계해도 안 갈린다.
> ⇒ **한쪽을 바깥에서 못박는 것 말고는 방법이 없다.**

### 한계 ② 어떤 축은 틀려도 티가 안 난다

비용이 **8축·60창을 통째로 뭉친 단일 RMS** 다(`cost_of`: `sqrt(mean(r²))`).
그래서 자극이 큰 축이 비용을 지배한다:

| | 값 | 결과 |
|---|---|---|
| `JDAMP` 를 틀렸을 때 / `ROTOR_I` 를 틀렸을 때 **RMS 가 변하는 양** | **1/16 ~ 1/87** | 감쇠를 크게 틀려도 RMS 가 거의 안 변한다 |
| **hip 이 비용에서 차지하는 비중** | **4 %** | hip 을 아무 값으로 둬도 통합 RMS 가 안 움직인다 |

⚠**hip 은 자유변수였는데도 초기값에서 안 움직였다.** 금지된 게 아니라 **정보가 없어서**다.
  겉보기엔 "다축이 정한 값" 인데 실제로는 각축값이 그대로 통과한 것이다(§1 의 ⚠ 표시).

### 한계 ③ 자유변수가 **모델오차를 흡수한다**

지연 `T_d` 를 자유롭게 두자 실측 밖으로 달아나면서 다른 값을 끌고 갔다:

| 탐색범위 | 추정 T_d | RMS | ROTOR_I 오차 |
|---|---|---|---|
| 2 ~ 16 ms (자유) | **12.83 ms** ← 실측 8.39±0.79 **밖** | 0.0615 | **−6.7 %** |
| 6.8 ~ 10 ms (실측으로 묶음) | 9.46 ms | **0.0449** (−27%) | **+1.5 %** (4.5배 개선) |

**묶었더니 RMS 도 좋아지고 물리값도 맞았다.** 원문이 `T_d` 를 자유로 두는 건 그걸 따로
재지 않아서다 — **우리는 쟀으니 묶는다.** 실측이 있는 값을 자유로 두면 나머지가 그리로 흐른다.

⇒ 여기서 **"RMS 가 낮다 ≠ 적합이 좋다"** 가 드러난다. 이 교훈은 따로 뺀 구간 검증에서 한 번 더
  반복된다(§5 — 적합에 안 쓴 구간으로 검증해도 네 번 연속 틀린 구조를 골랐다).

---

### 각축이 준 것 — 그리고 각축도 못 하는 것

각축(`--solo`)은 **한 축만 τ_ff 로 흔들고 나머지를 잡는다.** 다축이 못 가르는 것을
바깥에서 못박는 역할이다.

| 각축이 준 값 | 어떻게 | 다축의 어느 한계를 메웠나 |
|---|---|---|
| **`ROTOR_I` 7.327e-4** | τ_ff 경로(순환 없음). foot 7.327e-4 · calf 공통속도법 7.340e-4 → **0.17% 일치** | ③ 초기값을 참값에 놓음 |
| **foot `damping` 0.110** | 토크시험 q̈ 대 q̇ 크기 직접회귀(HL 0.12 · HR 0.10) | **① 맞바꿈을 밖에서 끊음** |
| 쿨롱마찰(스윕·토크절편) | 저속 평탄부 / 절편 q̇→0 외삽 | ① 탐색범위·초기값 |
| **`T_d` 8.39±0.79 ms** | 왕복지연 직접 실측 | **③ 경계를 좁힘** |

**⚠각축도 만능이 아니다:**

- **`thigh` 관성은 원리적으로 못 잰다.** `--solo` 는 아래 calf+foot 이 매달린 **진자**다
  (주기 0.90 s). 런이 0.21~0.48 s 로 **주기의 39%** 라, `M⁻¹` 예측이 전제하는 t→0 극한이
  깨진다. 자유(0.189)와 고정(0.221) **사이 어디도 아닌 값**이 나온다 → CAD 를 쓴다.
- **중력이 큰 축이 오염된다.** hip \|τ_g\| 5.5 · thigh 6.35 Nm. 보정은 한 점에서 하는데
  시험은 ±53° 를 훑어 잔차가 방향 비대칭으로 남는다. hip 은 τ_trip 에도 걸려 **미측정**이다.
  ⇒ 남은 미해결(§9)이 전부 여기로 모인다.

---

### 그래서 최종값은 **출처가 셋으로 갈린다**

```
  각축 직접측정 (--solo)
        │   초기값(출발점) 과 탐색범위(허용 범위) 를 준다
        ▼
  다축 CMA-ES (9모수)
        │
        ├─ 자극이 있는 축 ──────▶ 다축이 정한 값     thigh · calf · foot
        └─ 자극이 없는 축(hip 4%) ▶ 각축값이 그대로   hip ⚠
        │
        ▼
  fit_v2  (최종)
```

| 고정의 정도 | 대상 | 뜻 |
|---|---|---|
| **완전 고정** | `T_d`(6.8~10 ms) · foot `damping` | 실측이 있으니 흘러가지 못하게 묶음 |
| **초기값 + 탐색범위**(×0.3~3) | `ROTOR_I` · 전 축 `JDAMP`/`JFRIC` | 각축값 중심으로 다축이 그 안에서 정함 |
| **사실상 고정** ⚠ | **hip** `JDAMP`·`JFRIC` | 자유변수인데 자극이 없어 초기값에 머묾 |
| **탐색범위 끝에 붙음** ★ | calf `JDAMP` · foot `JFRIC` | 탐색범위 아래 끝까지 밀렸다 — "찾은 값" 이 아니라 **"더 못 내려간 값"** |

⇒ **§1 의 표는 이 세 갈래의 결과**다. ⚠ 와 ★ 를 그렇게 읽을 것.

---

## 1. MJCF 에 넣을 값

| kind | `dof_armature` | `dof_damping` | `dof_frictionloss` |
|---|---|---|---|
| hip | 0.0359 | 0.090 ⚠ | 0.827 ⚠ |
| thigh | 0.0359 | 0.091 ❌ | 0.567 |
| calf | 0.0808 | **0** ✓ | 0.573 |
| foot | **0** → tendon | 0.110 | 0.240 |

`tendon_armature`(foot) = **0.0517** · `delay` = **8.60 ms**

### 네 번의 적합에서 얼마나 흔들렸나

| 값 | v2 | v3 | v4 | v5 | 산포 | 읽는 법 |
|---|---|---|---|---|---|---|
| `JFRIC.calf` | 0.572 | 0.586 | 0.573 | 0.573 | **2%** | ✅ 확정 |
| `JFRIC.foot` | 0.252★ | 0.252★ | 0.252★ | **0.240** | — | v5 만 자유 — 1점 |
| `JFRIC.thigh` | 0.506 | 0.553 | 0.533 | 0.567 | 11% | ⚠ 보통 |
| `delay`[ms] | 8.78 | 8.86 | 7.42 | 8.60 | 17% | 아래 참조 |
| `JDAMP.thigh` | 0.170 | 0.142 | 0.180 | 0.091 | **61%** | ❌ 못 믿는다 |
| `JDAMP.calf` | 0.0097★ | — | 0.0035★ | 0.0021★ | — | **매번 바닥 → 0** |

★ = 그 판에서 탐색범위 끝에 붙어 있었다(그 값은 "찾은 값" 이 아니다).

⇒ **`JDAMP.thigh` 가 2배로 흔들리는 동안 `JFRIC.thigh` 는 11% 안이다.** 둘의 합만
정해지고 나눔이 안 된다 — 한계 ①(r=+0.93)이 thigh 에서 그대로 살아 있다.

⇒ **`JDAMP.calf` 는 반대다.** 하한을 0.009→0.0018 로 5배 내려도 계속 바닥을 따라
내려갔는데(0.0097→0.0035→0.0021), 짝인 `JFRIC.calf` 는 **2% 도 안 움직였다.**
맞바꿈이면 짝이 같이 움직여야 한다. 안 움직인다 ⇒ **calf 점성감쇠는 정말 0 이다.**
탐색범위 인공물이 아니라 결과다.

⇒ **`delay` 가 이 적합이 더 물리적이라는 독립 근거다.** 마찰 탐색범위가 잘못 좁았던
v4 에서 7.42 ms 로 흘렀다가(하한 19%), 제대로 열자 **8.60** 으로 돌아왔다 —
직접 실측 8.39±0.79 와 0.21 ms 차이다. 자유변수가 모델오차를 흡수하다 만 것이다.

> **tendon 이란** — 밧줄이 아니라 **관절 두 개를 하나로 묶는 MuJoCo 장치**다.
> `<fixed>` 로 `1·q_calf + 1·q_foot` 를 정의하면 그 합이 곧 **실기 ch3 모터의 엔코더 각**
> (raw 각)이 된다. calf 만 돌려도 ch3 엔코더가 변하는 실제 현상 그대로다.
> 액추에이터를 여기 붙이면 힘이 두 관절로 자동 분배되고(`τ_calf`·`τ_foot` 둘 다),
> 로터 관성도 (calf, foot) **비대각**으로 걸린다 — `dof_armature` 로는 못 만드는 항이다.

⚠ 고정한 값(식별 안 됨) ★ 탐색범위 끝(더 낮을 수 있음)

```
armature = ROTOR_I · N²    ROTOR_I = 7.327e-4 ± 10%    N = 7 · gear_k
⚠ foot 은 armature 를 tendon 으로 옮긴다 — dof_armature[foot]=0 안 하면 이중 계상
```

### 값마다 얼마나 믿을 수 있나

| 값 | 출처 | 오차 | 믿을 수 있나 |
|---|---|---|---|
| `ROTOR_I` 7.327e-4 | 각축 토크시험 4곳 | **±10%** | ✅ 좋다 |
| `damping.foot` 0.110 | 각축 토크시험 좌우 | **±0.012** (11%) | ✅ 좋다 |
| `delay` 8.78 ms | 직접 실측 8.39±0.79 | **±0.79** | ✅ 좋다 |
| `frictionloss.calf` 0.573 | PACE 4판 | **±2%** | ✅ 좋다 |
| **`damping.calf` 0** | PACE 4판 전부 바닥, 짝은 불변 | 상한 0.002 | ✅ **0 으로 확정** |
| `frictionloss.thigh` 0.567 | PACE 4판 | ±11% | ⚠ 보통 |
| `frictionloss.foot` 0.240 | PACE(v5 에서 처음 자유) | 1점뿐 | ⚠ 보통 |
| `damping.thigh` 0.091 | PACE 4판 **0.091~0.180** | **±61%** | ❌ 못 믿는다 |
| `damping.hip` 0.090 | 없음 — 옛 외삽값 | 미측정 | ❌ 못 믿는다 |
| `frictionloss.hip` 0.827 | 위치스윕(고정) | 미검증 | ❌ 못 믿는다 |

**❌ 넷은 도메인 랜덤화 대상으로 둘 것.**

---

## 2. CAD 가 얼마나 안 맞나 (축별)

실측 관성 vs **그 자세의 다물체 예측**.

| 축 | 예측 | 실측 | 오차 |
|---|---|---|---|
| HL_calf | 0.0991 | 0.1073 | **+8.3%** |
| HR_calf | 0.0991 | 0.1046 | **+5.5%** |
| HL_foot | 0.0533 | 0.0489 | **−8.3%** |
| HR_foot | 0.0533 | 0.0545 | **+2.2%** |
| HL_thigh | 0.1887 | 0.2479 | +31% ❌ 원리적 한계 |
| HL/HR_hip | — | — | 미측정 |

**calf·foot 넷이 ±8.3% 안이고 부호가 갈린다** ⇒ 계통오차가 아니라 산포.
**CAD 질량·관성은 맞다.**

⚠**thigh 는 `--solo` 로 못 잰다 — 원리적 한계다** (2026-08-14 확인).
중력창을 넣어 방향편차를 15.8% → **2.6%** 로 잡았는데도 +31% 가 남는다. 이유:

> `--solo` 는 아래 calf+foot 이 매달린 **진자**다. 주기 ≈ 2π√(L/g) = **0.90 s** 인데
> 런 길이가 0.21~0.48 s — **주기의 39%** 다. `M⁻¹` 예측은 "모든 자유 dof 가 같이
> 가속" 하는 **t→0 극한**인데, 그만큼 긴 런에서는 calf 가 뒤처지고 thigh 가 그
> 관성을 끌고 간다. 자유(0.189)와 고정(0.221) 사이 어디도 아닌 값이 나온다.

⇒ **thigh·hip 관성은 CAD 를 쓴다.** calf·foot 넷이 ±8.3% 로 CAD 를 검증했고, thigh 의
CAD 도 같은 URDF 에서 왔다. 굳이 재려면 아래 링크를 **물리적으로 고정**해야 한다
(홀드는 유한 강성이라 안 된다 — §2 참조).

---

## 3. 모터 게인 — **바꿀 필요 없다**

배포 게인을 식별된 관성으로 평가하니 전부 정상 범위다.

| kind | kp [채널] | kd [채널] | f_n | ζ | 판정 |
|---|---|---|---|---|---|
| hip | 100 | 6.0 | 3.5 Hz | 0.66 | ✅ |
| thigh | 50 | 4.0 | 2.7 Hz | 0.69 | ✅ |
| calf | 80 | 3.5 | 5.3 Hz | 0.73 | ✅ |
| foot | 30 | 2.0 | 4.5 Hz | 0.94 | ✅ 약간 과감쇠 |

foot 만 ζ 0.94 로 조금 높다. 굳이 맞추려면 **kd 2.0 → 1.5**. 급하지 않다.

시험용 게인은 별도다 — 식별 처프는 kp 를 낮추고(궤적이 명령에 붙으면 파라미터
정보가 사라진다), 홈복귀는 배포 게인을 쓴다.

---

## 4. 더 해야 할 시험

| 순위 | 무엇 | 어디서 | 비용 | 얻는 것 |
|---|---|---|---|---|
| **1** | ⑦ 재수집 — **진폭** 확대 | 실기 | 30분 + 안전확인 | `JDAMP.thigh`(±61%) 분해 |
| **2** | ⑥ `coef` 경사계 측정 | 실기 | 10분 | 마지막 미측정 구조값 |
| 3 | ①-b 중력 최소자세에서 관성 | 실기 | 1시간 | `damping.hip` 등 |
| 4 | ④ 다른 궤적 검증 · ⑤ hip 좌우차 | 실기 | — | 신뢰도 |

⓪ · ⓪-b · ① 은 **끝났다**(백래시 · `coef` · 탐색범위 확장). 결론만 아래에 남긴다.

### ⓪-b ~~`coef` 크기 식별~~ — **해봤다. PACE 로는 안 된다** (2026-08-14)

`coef` 를 탐색변수로 넣었다(`pace_cmaes.py`, `set_coupling_coef` + `retarget_coupling`).
`q_raw` 는 coef 가정과 무관하게 정확하므로 `q_foot_true = q_raw − c·q_calf` 로 다시 풀어
비교한다. `c=1` 이 항등이라는 것부터 확인했다 — 기존 결과가 소수 4자리까지 재현됐다.

**결과: `c` 를 자유로 풀면 오히려 나빠진다.**

| | 적합 | 따로 뺀 구간 | `coef` |
|---|---|---|---|
| `fit_v2` — `coef` 고정 1.000 | **0.4050** | **0.3967** | 1.000 |
| `fit_v3` — `coef` 자유 | 0.4058 | 0.3983 | 1.064 |

차원만 하나 늘고 얻은 게 없다. **데이터는 `c = 1.0` 을 반박하지 않는다** —
그대로 두면 된다. (탐색범위 0.70~1.15 는 코드에 남겨 뒀다. 기본은 `--pin coef` 고정.)

> ★**중간에 오판이 있었다 — 이 교훈이 결과보다 중요하다.**
> `x0` 에서 `c` 를 1-D 로 훑었더니 최소가 0.76 이었고, 따로 뺀 구간도 −7.6% 로 따라왔다.
> `JDAMP.calf` 와 맞바꿈인지 격자로 확인했더니 **네 행 전부 최소가 `×0.5`** 였고,
> 그걸 *"coef 와 무관하다 = 독립이다"* 로 읽어 "맞바꿈 아님, 결정적" 이라고 보고했다.
> **틀렸다.** `×0.5` 는 훑은 범위의 **끝**이었다. 진짜 최적은 `JDAMP.calf = 0.0097`
> (`×0.108`)로 격자 밖이었고, 거기까지 열어 주면 calf 잔차가 그쪽으로 흡수되어
> `coef` 는 할 일이 없어진다.
> ⇒ *"탐색범위 벽에 박힌 값은 식별된 값이 아니다"* 는 **격자 훑기에도 똑같이 적용된다.**
> `tests/scan_coef_jdamp.py` 에 행 최소가 격자 경계면 판정을 보류하는 가드를 넣었다.

`coef` 크기를 정말 재려면 **엔코더 밖**으로 나가야 한다 — ⑥ 참조.

### ① ~~`JDAMP.calf`·`JFRIC.foot` 탐색범위~~ — **끝났다** (2026-08-14, `fit_v5`)

열어 보니 셋이 갈렸다:

| | 결과 |
|---|---|
| `JFRIC.foot` | 0.2516(벽) → **0.240, 탐색범위 17%** — 값이 됐다. 실측(0.36)보다 낮은 게 맞다(Stribeck) |
| `JFRIC.calf` | 하한을 0.533→0.190 으로 내렸는데 **0.573 그대로** — 애초에 벽이 아니었다 |
| `JDAMP.calf` | 하한 5배를 내려도 계속 바닥 ⇒ **0 으로 확정** (위 산포표) |

성적도 네 판 중 최고다(적합 0.4041 · 따로 뺀 구간 **0.3937**).

⚠**두 번의 헛다리를 짚었다.** ⓐ `JFRIC_SPAN_DN` 을 넓혔는데 실측이 둘 다 있는
kind 는 `_span_lo` 의 −30% 하드코딩이 덮어써서 thigh·calf·foot 에 **안 먹었다**
(`fit_v4` 의 하한이 `fit_v2` 와 글자 그대로 같았던 게 증거였다).
ⓑ `box_report` 가 선형 위치를 써서 500배 범위인 `JDAMP` 에서 거짓 경보를 냈다.
둘 다 고쳤다(`--pin 라벨=값` 도 같이 넣었다 — 적합이 알려준 값에 못박을 수 있다).

<details><summary>당시 지시(참고)</summary>

둘 다 `fit_v2`·`fit_v3` 에서 **매번 바닥(0%)** 이다. 벽에 박힌 값은 식별된 값이 아니므로
지금 표의 `damping.calf 0.009` · `frictionloss.foot 0.252` 는 **값이 아니라 하한**이다.

한 방향으로만 눌리고 반대쪽 짝은 고정/범위 안에 있어서, 둘이 같이 미끄러지는
맞바꿈이 아니다 — 열어도 발산하지 않는다:

| 벽에 박힌 값 | 반대쪽 짝 | 뜻 |
|---|---|---|
| `JDAMP.calf` 0.0097 (벽) | `JFRIC.calf` 범위 안(7%) | calf 손실이 **쿨롱 지배적**, 점성 ≈ 0 |
| `JFRIC.foot` 0.2516 (벽) | `JDAMP.foot` 실측 0.110 **고정** | 발목 마찰이 **실측보다 낮다** |

`pace_cmaes.py` 의 탐색범위를 넓혀 뒀다(`JDAMP_SPAN` 하한 0.10→0.02 ·
`JFRIC_SPAN_DN` 0.50→0.75). **노트북에서 다시 돌리면 된다:**

```
.venv/bin/python biped/emb/pace/pace_cmaes.py \
    biped/emb/pace/results/pace_multichirp_f0.4_dqfix.npz \
    --holdout 0.2 --holdout-mode interleave \
    --pin hip,rotor,JDAMP.foot,coef --out fit_v4_wide
```

이번엔 두 값이 **범위 안에서 정해지는지**를 본다. 또 벽이면 물리적 하한(0)이라는 뜻이다.

</details>

앞으로 적합을 돌릴 때는 calf 점성을 0 에 못박는다 — 차원 하나가 준다:

```
--pin hip,rotor,JDAMP.foot,coef,JDAMP.calf=0
```

### ①-b 중력이 작은 자세에서 관성 재기 — **값의 완결성**

⚠**정확도에는 거의 영향이 없다** — hip·thigh 는 합쳐서 오차의 2~4% 뿐이다.
값이 비어 있는 걸 채우는 작업이지 RMS 를 줄이는 작업이 아니다.

미해결 셋(`hip 관성`·`thigh 관성`·`calf 감쇠`)이 **전부 같은 원인**이다. 중력이 큰 축은
중력 잔차가 방향 비대칭으로 남아 게이트를 못 넘는다(thigh |τ_g| 6.35 Nm · hip 5.5 Nm).

> 새 절차가 필요하다: 시험 자세를 **중력이 최소인 곳**으로 옮기고 거기서 훑는다.
> thigh 는 수직 근처, hip 은 롤축이 수평인 자세.

이게 되면 위 표의 ❌ 넷 중 **셋이 채워진다.**

### ⓪ ~~백래시~~ — **측정 완료, 범인이 아니다** (2026-08-14)

HL_foot 토션 히스테리시스: **백래시 0.0667° ± 0.0034 (채널)** · 강성 **14.55 Nm/deg**
(834 Nm/rad) · 주기간 강성 산포 1% — 깨끗한 측정이다.

| 후보 | 잔차 기여 | 판정 |
|---|---|---|
| 백래시 0.0556° (관절) | 0.03° (5%) | ✗ **12배 작다** |
| 감속기 유연성 | 공진 19.8 Hz — 처프 대역(≤0.62 Hz)의 32배 위 | ✗ 무관 |
| **`coef` 오차 3~5%** | **0.44~0.73°** | ★ **유일하게 자릿수가 맞는다** |

⇒ foot 잔차 0.68° 는 백래시로도 유연성으로도 설명 안 된다. **`coef` 가 남은 후보다.**

⚠**calf 는 못 쟀다** — `평탄 구간 미검출 · 강성 —  · 루프폭 1.478°`.
시험이 성립하지 않았다. 로그가 이유를 보여준다: **중력보정 폭 1.101 Nm 이 시험 진폭
±0.45 Nm 의 2.4배**다. 보정 오차가 얹히면 파단(0.665~0.719)을 넘어 축이 미끄러진다.
루프폭 1.478° 가 회전 감시 한계 1.5° 에 거의 닿은 것이 그 증거다.
⇒ calf 백래시를 재려면 **중력이 작은 자세**가 먼저다(§4-①-b 와 같은 문제).

### ② 마찰 분해 — 속도를 독립 제어하는 시험

`damping`↔`frictionloss` 가 서로 맞바꿔져(r=+0.93) **총량은 맞는데 나눔이 안 된다**.
지금 데이터는 τ 범위가 1.75배뿐이라 못 가린다.

> **일정 속도로 유지하며** 토크를 재는 시험이 필요하다. 속도를 몇 단계로 바꾸면
> τ(q̇) 곡선이 직접 나오고, **절편이 `frictionloss` · 기울기가 `damping`** 이다.

### ③ HR_thigh 관성 (미측정)

좌우 대조용. ①이 되면 같이 하면 된다.

### ④ 다른 궤적으로 검증 한 번 더

지금 검증은 **게인**만 바꿨다. 궤적(진폭·주파수)도 바꾼 데이터가 있으면 더 강하다.

```bash
python3 collect_multichirp.py --rate 400 --f-scale 0.6 --gains validate
```

⚠`--rate 400` 권장 — 500 Hz 에서 루프 밀림이 6.8%(최대 44 ms)까지 갔다.

### ⑤ hip 좌우차 확정

위치스윕은 16% 차(0.928/0.803), 토크모드는 4% 차(0.888/0.852). 어느 쪽이 맞는지 미결.
구조가 다르다는 가설(HR 샤프트 경유 · HL 직결)을 확인하려면 반복 측정이 필요하다.

### ⑥ `coef` 크기 — 경사계로 재기 (**엔코더 밖으로 나가는 유일한 길**)

`couple_check.py` 로는 **원리적으로 못 잰다.** 2026-08-14 실기 결과가 케이스 **(A)** 다:

| | 무릎 스윙 | `foot_ch` 기울기 |
|---|---|---|
| HL | 115.9° | +0.0001 |
| HR | 148.9° | +0.0000 |

엔코더가 모터축에 있어 커플링을 아예 못 본다(`\|c\| < 2e-4`). 기울기가 실제 크기와
무관하게 0 이므로 이 도구는 (A)/(B) 만 가르고 크기는 못 낸다.
*(다만 이 결과가 **raw각 PD 법칙이 옳다**는 걸 게인 2배 검증과 독립적으로 뒷받침한다 —
엔코더 좌표가 곧 tendon 좌표다.)*

절차:

1. 발목을 **채널각 고정**으로 잡는다 ← 결정적. 평소 명령경로로 잡으면 `coef=1` 보상이
   걸려 재려는 대상이 정확히 상쇄된다. 2026-08-10 에 부호를 반대로 판정한 게 이 함정이다
2. 무릎만 크게(100° 이상) 돌린다
3. 발바닥 절대각을 휴대폰 경사계로 읽는다

`Δ발_절대각 = Δq_calf·(1 − c)` 이므로 100° 스윙에서:

| `coef` | 발바닥 절대각 변화 |
|---|---|
| 1.00 | **0°** |
| 0.90 | 10° |
| 0.76 | 24° |

경사계 분해능 0.5° 면 넉넉하다. 2026-08-10 의 *"육안 일치"* 는 무릎을 30° 만 돌렸다면
`c=0.9` 여도 3° 라 놓칠 수 있는 크기다 — **육안 관측은 `c=1` 의 증거로 약하다.**

### ⑦ 재수집 — **주파수보다 진폭이다**

지금 데이터의 calf 항별 크기:

| 항 | 크기 | 비중 |
|---|---|---|
| 관성 `I·q̈` (0.112 × 18.7 rad/s²) | 2.09 Nm | 82% |
| 쿨롱 `τ_c` (실측) | 0.39~0.58 Nm | 15% |
| **감쇠 `b·q̇`** (0.09 × 0.80 rad/s) | **0.072 Nm** | **3%** |

감쇠가 마찰의 1/8 이다. `JDAMP`↔`JFRIC` 맞바꿈(r=+0.93)도, `JDAMP.calf` 가 벽에 박히는
것도 전부 여기서 온다. 무엇을 키우느냐로 비용이 갈린다:

| | `q̇` | `q̈` | 감쇠/마찰 | `coef` 관측성 | **최대토크** |
|---|---|---|---|---|---|
| 진폭 ×2 | ×2 | ×2 | ×2 | ×2 | **×2** |
| 주파수 ×2 | ×2 | ×4 | ×2 | ×4 | **×4** |

1순위가 감쇠이므로 **진폭이 토크당 2배 효율적**이다. 여유도 그쪽에 있다:

| 축 | 현 진폭 | 단측여유 | 배율 |
|---|---|---|---|
| hip | 5° | 6.0° | **1.2×** ← 없음 |
| thigh | 18° | 40.0° | 2.2× |
| calf | 16° | 40.0° | 2.5× |
| foot | 13° | 23.2° | 1.8× |

제안: thigh 18→32° · calf 16→32° · foot 13→20°, **hip 만 진폭 여유가 없으니
`f_end` 1.8→2.6 Hz**. calf 최대토크 2.1→4.2 Nm (`tau_trip` 16 · 유연성 공진 19.8 Hz).

⚠먼저 확인할 것: **thigh 32° + calf 32° 동시 스윙이 지그·바닥과 안 부딪히는지**.
그리고 래치오프는 원위축에서 나던 미해결 문제인데 토크 2배는 그 위험을 키운다.
검증셋(`kp_validate`)도 같이 다시 받아야 비교가 성립하고, 진폭이 커지면 RMS(°) 자체가
커지므로 **데이터셋 간 비교는 정규화해야 한다.**

---

## 5. 확정된 구조 (참고)

| 항목 | 결론 |
|---|---|
| calf→foot 커플링 | **실재, `coef` = 1 로 둔다** — 자유로 풀면 1.064 로 가지만 성적이 나빠진다 |
| 엔코더 | **모터축(raw각)** — 커플링을 안 본다 (케이스 A, 기울기 +0.0000/+0.0001) |
| foot PD 오차 | **raw각** — 게인 2배 검증 + 케이스 A, **독립 근거 둘** |
| MJCF | `<fixed>` tendon, coef 1·1 |

### 적합 성적

| | 값 | 뜻 |
|---|---|---|
| 적합 RMS | 0.404° | 파라미터를 찾는 데 쓴 48창 |
| 따로 뺀 구간 RMS | 0.394° | 안 쓴 12창 — **적합보다 낮다**(과적합 없음) |
| **검증 RMS** | **0.251°** | **게인 2배로 따로 수집한 데이터** — 가장 강한 시험 |

★**교훈 1**: 적합에 안 쓴 구간으로 검증해도 네 번 연속 **틀린 구조**를 골랐다. 게인을 바꾼 검증이
뒤집었다. 구조는 구조 증거로 고르고, 숫자로 가리려면 **조건을 바꾼 데이터**가 필요하다.

★**교훈 2** (2026-08-14): *"탐색범위 벽에 박힌 값은 식별된 값이 아니다"* 는 **적합 결과뿐 아니라
진단용 격자 훑기에도 적용된다.** `coef` 맞바꿈 진단에서 네 행 전부 최소가 격자 끝이었는데
그걸 *"행마다 최적이 같다 = 독립이다"* 로 읽고 "결정적" 이라고 보고했다. 그건 물리가 아니라
격자 경계였다. **부분 훑기의 결론은 그 범위가 최적을 담고 있을 때만 유효하다.**
